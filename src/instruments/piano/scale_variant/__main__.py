"""Entry point for `python3 -m instruments.piano.scale_variant`: importing `main` runs its top-level generation
script (every scale followed by the same scale a half-tone away, see `main.py`)."""
from instruments.piano.scale_variant import main
