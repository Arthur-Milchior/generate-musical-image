"""Entry point for `python3 -m instruments.piano.progression`: importing `main` runs `main.py`'s top-level
code, which generates every chord progression and writes the corresponding Anki notes CSV (see `main.py` and
`generate.py`)."""
print("main progression")

from instruments.piano.progression import main


