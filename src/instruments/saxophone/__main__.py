"""Entry point for `python3 -m instruments.saxophone`: runs `generate.py`, which renders every saxophone
fingering to SVG and writes the corresponding Anki notes CSV (see `generate.py` and `../../README.md`)."""
from instruments.saxophone import generate

print("generated")