"""Entry point for `python3 -m instruments.harmonica`: importing `__init__` runs the module-level generation
loop in `__init__.py`, which (re)writes all the harmonica diagram SVGs under `images/`."""
import __init__
