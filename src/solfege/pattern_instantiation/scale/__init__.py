"""Package marker for `solfege.pattern_instantiation.scale`; deliberately empty. Import `scale`/`chromatic_scale`
for the `Scale`/`ChromaticScale` classes -- see this package's parent for the shared instantiation machinery."""
