"""Package marker for `solfege.pattern_instantiation`; deliberately empty. This package anchors the
note-less patterns from `solfege.pattern` (`ChordPattern`, `ScalePattern`, `InversionPattern`) to a concrete
lowest note, producing the `Chord`/`Scale`/`InversionInstantiation` (and their chromatic-only counterparts)
classes used elsewhere in the app. Import the specific submodule/subpackage you need -- see this folder's
README.md for an overview."""
