"""Package marker for `solfege.pattern.inversion`; deliberately empty. Import `inversion_pattern` for the
`InversionPattern` class (see this folder's README.md); instances are normally created and registered
automatically by `ChordPattern`, not by importing this package directly."""
