"""Package marker for `solfege.pattern.scale`; deliberately empty. Import `scale_pattern` for the
`ScalePattern` class, or `scale_patterns` to populate the scale catalog (see this folder's README.md)."""
