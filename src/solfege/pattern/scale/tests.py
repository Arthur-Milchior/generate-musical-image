"""Dead/unused module -- not imported anywhere; superseded by the standard `unittest` tests in this folder
(`test_scale_pattern.py`) and in `solfege.pattern_instantiation.scale` (`test_scale.py`). The commented-out
code below shows an earlier doctest-style mechanism (`utils.util.tests_modules`) for running module
docstring tests instead."""

# from solfege.pattern.scale import scale
# from solfege.pattern.scale import scale_pattern
# from utils.util import tests_modules

# tests_modules([scale, scale_pattern])